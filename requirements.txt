Abg==2.3.1               # Check if this is the latest version or update if necessary
pyrogram==2.0.106        # Check if this is the latest version or update if necessary
pymongo==4.5.0
motor==3.2.0
requests==2.31.0
dnspython==2.4.2
tgcrypto==1.2.2          # Check if this is the latest version or update if necessary
asyncio==3.4.3
python-dotenv==1.0.0
lexica-api==0.1.1
deep-translator==1.10.1
pycgi==2.0.3
apscheduler==3.10.1
Flask==3.0.0
uvloop==0.18.0
psutil==5.9.5
langdetect==1.0.9
MukeshAPI==1.0.0         # Check if this is the latest version or update if necessary
speedtest-cli==2.1.3
aiohttp==3.8.5

# New Packages for Enhanced Features
flask-cors==3.0.10       # Handle CORS in Flask applications
gunicorn==20.1.0         # WSGI HTTP Server for UNIX, serving the Flask app in production
pydantic==1.10.7         # Data validation and settings management using Python type annotations
fastapi==0.100.0         # Modern, fast (high-performance) web framework for building APIs
httpx==0.24.1            # HTTP client for Python 3
websockets==11.0.3       # Handle WebSocket connections for real-time communication
